# Retired on 2026-09-21

This frontend is disabled and retained for reference. Use [New Workspace](../new-workspace/README.md), Alfred `ns`, or `workspace create "Name"` instead. The old orchestrator source remains at `~/Code/pjws`; existing project data and tmux sessions are preserved. Its CLI and login hook are not installed.

The directory remains in place because macOS refused to move it, including outside the sandbox. `build.sh` skips directories containing `ARCHIVED.md`. The installed Alfred symlink still points here, but the workflow is disabled and exposes no active keywords.
